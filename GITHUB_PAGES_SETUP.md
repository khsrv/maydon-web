# Настройка GitHub Pages для Telegram Mini App

Это руководство поможет вам настроить автоматический деплой Flutter веб-приложения на GitHub Pages.

## 📋 Что было сделано

✅ Создан файл `.github/workflows/deploy.yml` для автоматического деплоя

## 🚀 Пошаговая инструкция

### Шаг 1: Создание GitHub репозитория

1. Перейдите на [GitHub](https://github.com) и войдите в аккаунт
2. Нажмите кнопку **"New repository"** (или перейдите по ссылке: https://github.com/new)
3. Заполните:
   - **Repository name**: `maydon` (или другое имя)
   - **Description**: описание проекта (опционально)
   - **Visibility**: Public (GitHub Pages работает только с публичными репозиториями на бесплатном плане)
   - **НЕ** инициализируйте с README, .gitignore или лицензией (если у вас уже есть код)
4. Нажмите **"Create repository"**

### Шаг 2: Инициализация Git (если еще не сделано)

Если проект еще не является Git репозиторием:

```powershell
# Инициализация Git
git init

# Добавление всех файлов
git add .

# Первый коммит
git commit -m "Initial commit"
```

### Шаг 3: Подключение к GitHub репозиторию

```powershell
# Добавьте remote (замените YOUR_USERNAME и YOUR_REPO на ваши данные)
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git

# Переименуйте ветку в main (если нужно)
git branch -M main

# Отправьте код на GitHub
git push -u origin main
```

### Шаг 4: Настройка base-href (важно!)

**Если ваш репозиторий называется НЕ "maydon":**

1. Откройте файл `.github/workflows/deploy.yml`
2. Найдите строку с `flutter build web --release`
3. Измените на:

```yaml
- name: Build web
  run: flutter build web --release --base-href "/YOUR_REPO_NAME/"
```

Замените `YOUR_REPO_NAME` на имя вашего репозитория.

**Примеры:**
- Репозиторий `my-app` → `--base-href "/my-app/"`
- Репозиторий `maydon` → `--base-href "/maydon/"`
- Если используете пользовательский домен → `--base-href "/"`

### Шаг 5: Включение GitHub Pages

1. Перейдите в ваш репозиторий на GitHub
2. Откройте **Settings** (Настройки)
3. В левом меню найдите **Pages**
4. В разделе **Source**:
   - Выберите **Deploy from a branch**
   - Branch: выберите **gh-pages**
   - Folder: выберите **/ (root)**
5. Нажмите **Save**

### Шаг 6: Запуск деплоя

Есть два способа:

#### Способ 1: Автоматический (при push в main)

Просто отправьте изменения в ветку `main`:

```powershell
git add .
git commit -m "Deploy to GitHub Pages"
git push origin main
```

GitHub Actions автоматически соберет и задеплоит приложение.

#### Способ 2: Ручной запуск

1. Перейдите в ваш репозиторий на GitHub
2. Откройте вкладку **Actions**
3. Выберите workflow **"Deploy Flutter Web to GitHub Pages"**
4. Нажмите **"Run workflow"**
5. Выберите ветку `main` и нажмите **"Run workflow"**

### Шаг 7: Получение URL

После успешного деплоя:

1. Перейдите в **Settings** → **Pages**
2. Вы увидите URL вида: `https://YOUR_USERNAME.github.io/YOUR_REPO_NAME/`

**Примеры:**
- `https://username.github.io/maydon/`
- `https://username.github.io/my-app/`

### Шаг 8: Проверка деплоя

1. Дождитесь завершения workflow (обычно 3-5 минут)
2. Проверьте статус в **Actions** → должен быть зеленый ✅
3. Откройте URL вашего сайта в браузере
4. Убедитесь, что приложение загружается корректно

## 🔧 Настройка для пользовательского домена (опционально)

Если у вас есть свой домен:

1. Создайте файл `CNAME` в корне проекта:
   ```
   yourdomain.com
   ```

2. Обновите `.github/workflows/deploy.yml`:
   ```yaml
   - name: Build web
     run: flutter build web --release --base-href "/"
   ```

3. В настройках GitHub Pages укажите ваш домен

4. Настройте DNS записи у вашего регистратора домена:
   - Тип: `CNAME`
   - Имя: `@` или `www`
   - Значение: `YOUR_USERNAME.github.io`

## 📝 Важные замечания

### Base-href

**Критически важно** правильно настроить `base-href`:

- ✅ **Правильно**: `--base-href "/maydon/"` (если репозиторий "maydon")
- ✅ **Правильно**: `--base-href "/my-repo/"` (если репозиторий "my-repo")
- ❌ **Неправильно**: `--base-href "/maydon"` (без слеша в конце)
- ❌ **Неправильно**: `--base-href "maydon/"` (без слеша в начале)

### Проверка base-href

После деплоя проверьте:
1. Откройте консоль браузера (F12)
2. Проверьте, что все ресурсы загружаются с правильного пути
3. Если видите ошибки 404, значит base-href настроен неправильно

### Обновление приложения

Каждый раз при обновлении кода:

```powershell
git add .
git commit -m "Update app"
git push origin main
```

GitHub Actions автоматически задеплоит новую версию.

## 🐛 Решение проблем

### Проблема: Workflow не запускается

**Решение:**
- Убедитесь, что файл `.github/workflows/deploy.yml` находится в правильном месте
- Проверьте, что вы отправляете изменения в ветку `main` или `master`

### Проблема: Сайт не открывается / 404

**Решение:**
- Проверьте, что base-href настроен правильно
- Убедитесь, что GitHub Pages включен и использует ветку `gh-pages`
- Подождите несколько минут после деплоя (DNS может обновляться)

### Проблема: Ресурсы не загружаются

**Решение:**
- Проверьте base-href в workflow файле
- Убедитесь, что все пути относительные
- Проверьте консоль браузера на ошибки

### Проблема: Workflow падает с ошибкой

**Решение:**
1. Откройте **Actions** → выберите упавший workflow
2. Посмотрите логи ошибки
3. Частые проблемы:
   - Неправильная версия Flutter (обновите в workflow)
   - Ошибки компиляции (исправьте код)
   - Проблемы с зависимостями (проверьте `pubspec.yaml`)

## 📚 Полезные ссылки

- [GitHub Pages Documentation](https://docs.github.com/en/pages)
- [GitHub Actions Documentation](https://docs.github.com/en/actions)
- [Flutter Web Deployment](https://docs.flutter.dev/deployment/web)

## ✅ Чеклист готовности

- [ ] GitHub репозиторий создан
- [ ] Код загружен в репозиторий
- [ ] Base-href настроен правильно
- [ ] GitHub Pages включен
- [ ] Workflow успешно выполнен
- [ ] Сайт открывается по URL
- [ ] Все ресурсы загружаются корректно
- [ ] URL добавлен в настройки Telegram бота

## 🎉 Готово!

После успешного деплоя используйте полученный URL в настройках Telegram бота (см. `TELEGRAM_MINI_APP_SETUP.md`).

