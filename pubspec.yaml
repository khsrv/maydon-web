name: postj
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ^3.9.0-155.0.dev

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  another_flushbar: ^1.12.30
  flutter_bloc: ^9.1.1
  get_it: ^8.2.0
  freezed_annotation: ^3.1.0
  go_router: ^16.1.0
  # drift: ^2.28.1
  path: ^1.9.1
  # flutter_gen: ^5.11.0


#flutter gen-l10n каманда для генерации новых слов
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2  
  shared_preferences: ^2.5.3
  flutter_secure_storage: ^9.2.4
  flutter_secure_storage_web: ^1.2.1
  dio: ^5.9.0
  json_annotation: ^4.9.0
  shimmer: ^3.0.0
  bloc: ^9.0.0
  # image_picker: ^1.1.2
  url_launcher: ^6.3.2
  webview_flutter: ^4.13.0
  webview_flutter_wkwebview: ^3.23.0
  connectivity_plus: ^6.1.5
  smooth_page_indicator: ^1.2.1
  is_first_run: ^1.0.0
  flutter_svg: ^2.0.13
  cached_network_image: ^3.4.1
  firebase_core: ^4.0.0
  cloud_firestore: ^6.0.0
  package_info_plus: ^8.1.1
  share_plus: ^10.1.2
  flutter_native_splash: ^2.4.6
  flutter_launcher_icons: ^0.14.4
  js: ^0.6.7


  # flutter pub run flutter_native_splash:create


dev_dependencies:
  flutter_test:
    sdk: flutter

# dart run build_runner build --delete-conflicting-outputs

  build_runner: ^2.4.6
  drift_dev: ^2.28.1
  flutter_lints: ^6.0.0
  json_serializable: ^6.10.0
  freezed: ^3.2.0

  # flutter pub run flutter_native_splash:create

flutter_native_splash:
  color: "#FFFFFF"
  image: assets/images/logo/app_icon.png

  
# command to generate the app icon: flutter pub run flutter_launcher_icons:main

flutter_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/images/logo/app_icon.png"
  remove_alpha_ios: true


  # remove_alpha_ios: true

flutter:
  # Встроенная локализация через gen_l10n
  generate: true
  uses-material-design: true
  assets:
    - assets/images/
    - assets/images/logo/
    - assets/icons/
  fonts:
    - family: Gilroy
      fonts:
        - asset: assets/fonts/Graduate/Graduate-Regular.ttf
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/custom-fonts/#from-packages

